import React from 'react';

import Layout from '../components/Layout';

const ContactPage = () => (
  <Layout fullMenu>
    <article id="main">
    <header>
        <h2>Contact</h2>
      </header>
    </article>
  </Layout>
);

export default ContactPage;